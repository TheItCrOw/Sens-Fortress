<UserControl x:Class="SensFortress.View.Main.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SensFortress.View.Main.Views"
             xmlns:models="clr-namespace:SensFortress.Models.ViewModels;assembly=SensFortress.Models"
             xmlns:vms="clr-namespace:SensFortress.View.Main.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1100"           
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <!--Background Image-->
        <!--<Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Images/Home-Bonfire.jpg" RenderOptions.BitmapScalingMode="Fant" Stretch="None"/>
        </Grid.Background>-->

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Header Title-->
        <materialDesign:Card Height="50">
            <DockPanel>
                <Button Style="{StaticResource MaterialDesignRaisedLightButton}" Width="50" VerticalAlignment="Stretch" ToolTip="Resource name: MaterialDesignRaisedLightButton">
                    _LIGHT
                </Button>
            </DockPanel>
        </materialDesign:Card>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!--left side menu here-->
            <StackPanel>
                <TextBlock Style="{StaticResource InformationTextblockStyle}" Margin="10" HorizontalAlignment="Center" Text="Structure"/>
                <TreeView ItemsSource="{Binding RootNodes}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vms:BranchViewModel}" ItemsSource="{Binding SubBranches}">
                            <TextBlock Text="{Binding Name}" Margin="3 2">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource CategoryTextblockStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=TreeType}" Value="{x:Static models:TreeDepth.Branch}">
                                                <Setter Property="FontSize" Value="17"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=TreeType}"  Value="{x:Static models:TreeDepth.Leaf}">
                                                <Setter Property="FontSize" Value="17"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </StackPanel>

            <!--Gridsplitter 1-->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="DarkGray"/>

            <!--middle menu-->
            <ListView ItemsSource="{Binding Items1}" Grid.Column="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Code}" Header="Name"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Created"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Password" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!--Gridsplitter 2-->
            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="DarkGray"/>

            <!--Log here-->
            <materialDesign:Card Grid.Column="4" VerticalAlignment="Stretch">
                <StackPanel>
                    <TextBlock Style="{StaticResource InformationTextblockStyle}" Text="Task-log:"/>
                </StackPanel>
            </materialDesign:Card>

        </Grid>
    </Grid>
</UserControl>
