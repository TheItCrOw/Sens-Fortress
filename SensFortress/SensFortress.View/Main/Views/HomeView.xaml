<UserControl x:Class="SensFortress.View.Main.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SensFortress.View.Main.Views"
             xmlns:vms="clr-namespace:SensFortress.View.Main.ViewModel"
             xmlns:extensions="clr-namespace:SensFortress.View.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1100"           
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
            TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <!--Background Image-->
        <!--<Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Images/Home-Bonfire.jpg" RenderOptions.BitmapScalingMode="Fant" Stretch="None"/>
        </Grid.Background>-->

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Header Title-->
        <materialDesign:Card Height="50">
            <DockPanel>
                <Button Style="{StaticResource MaterialDesignRaisedLightButton}" Width="50" VerticalAlignment="Stretch" ToolTip="Resource name: MaterialDesignRaisedLightButton">
                    _LIGHT
                </Button>
            </DockPanel>
        </materialDesign:Card>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" MinWidth="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!--left side menu here-->
            <StackPanel>
                <materialDesign:Card materialDesign:ShadowAssist.ShadowDepth="Depth5">
                    <StackPanel>
                        <Expander>
                            <Expander.Header>
                                <TextBlock Style="{StaticResource InformationTextblockStyle}" Text="Structure"/>
                            </Expander.Header>
                            <DockPanel HorizontalAlignment="Stretch" Margin="30 0 30 0">
                                <materialDesign:PackIcon Kind="Search" VerticalAlignment="Top" Height="25" Width="25"/>
                                <TextBox FontStyle="Italic" FontSize="16" FontWeight="ExtraLight" HorizontalAlignment="Stretch" Margin="5 -15 0 10" materialDesign:HintAssist.Hint="search..." materialDesign:HintAssist.IsFloating="True"/>
                            </DockPanel>
                        </Expander>
                    </StackPanel>
                </materialDesign:Card>
                <extensions:ExtendedTreeView ItemsSource="{Binding RootNodes}" SelectedItem_="{Binding SelectedTreeViewItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0">
                    <extensions:ExtendedTreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type vms:TreeItemViewModel}" ItemsSource="{Binding Children}">
                            <DockPanel>
                                <!--Icon-->
                                <materialDesign:PackIcon VerticalAlignment="Center" Margin="0 0 5 0" Foreground="Gray">
                                    <materialDesign:PackIcon.Style>
                                        <Style TargetType="materialDesign:PackIcon">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=TreeType}" Value="{x:Static vms:TreeDepth.Root}">
                                                    <Setter Property="Kind" Value="FolderMultiple"/>
                                                    <Setter Property="Width" Value="28"/>
                                                    <Setter Property="Height" Value="28"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TreeType}" Value="{x:Static vms:TreeDepth.Branch}">
                                                    <Setter Property="Kind" Value="Folder"/>
                                                    <Setter Property="Width" Value="25"/>
                                                    <Setter Property="Height" Value="25"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TreeType}"  Value="{x:Static vms:TreeDepth.Leaf}">
                                                    <Setter Property="Kind" Value="Key"/>
                                                    <Setter Property="Width" Value="25"/>
                                                    <Setter Property="Height" Value="25"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </materialDesign:PackIcon.Style>
                                </materialDesign:PackIcon>
                                <!--Name textbox-->
                                <TextBox Text="{Binding Name}" Margin="3 2" Focusable="{Binding IsEditable}" Cursor="Arrow">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource TreeViewTextboxStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Path=TreeType}" Value="{x:Static vms:TreeDepth.Branch}">
                                                    <Setter Property="FontSize" Value="17"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=TreeType}"  Value="{x:Static vms:TreeDepth.Leaf}">
                                                    <Setter Property="FontSize" Value="17"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Path=IsEditable}"  Value="True">
                                                    <Setter Property="BorderThickness" Value="0 0 0 1"/>
                                                    <Setter Property="Background" Value="FloralWhite"/>
                                                    <Setter Property="Foreground" Value="Black"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                                <!--Edit button-->
                                <Button x:Name="EditTreeItemButton" Style="{StaticResource TreeViewButtonStyle}" Grid.Column="1" Margin="5 0 5 0" Command="{Binding EditTreeItemCommand}"
                                        Visibility="{Binding Path=IsSelected, Converter={StaticResource BoolToVisConverter}}">
                                    <materialDesign:PackIcon Kind="Edit" ToolTip="Edit"/>
                                </Button>
                                <!--Add branch button-->
                                <Button x:Name="AddBranchButton" Style="{StaticResource TreeViewButtonStyle}" Grid.Column="1" Margin="5 0 5 0" Command="{Binding AddTreeItemCommand}"
                                        Visibility="{Binding Path=MayHaveChildren, Converter={StaticResource BoolToVisConverter}}">
                                    <materialDesign:PackIcon Kind="FolderAdd" ToolTip="Add directory"/>
                                </Button>
                                <!--Add leaf button-->
                                <Button x:Name="AddLeafButton" Style="{StaticResource TreeViewButtonStyle}" Grid.Column="1" Margin="5 0 5 0" Command="{Binding AddTreeItemCommand}"
                                        Visibility="{Binding Path=MayHaveChildren, Converter={StaticResource BoolToVisConverter}}">
                                    <materialDesign:PackIcon Kind="KeyAdd" ToolTip="Add password"/>
                                </Button>
                                <!--Delete button-->
                                <Button x:Name="DeleteTreeItemButton" Style="{StaticResource TreeViewButtonStyle}" Grid.Column="1" Margin="5 0 5 0"
                                        Visibility="{Binding Path=IsSelected, Converter={StaticResource BoolToVisConverter}}">
                                    <materialDesign:PackIcon Kind="Delete" ToolTip="Delete"/>
                                </Button>
                            </DockPanel>
                        </HierarchicalDataTemplate>
                    </extensions:ExtendedTreeView.ItemTemplate>
                </extensions:ExtendedTreeView>
            </StackPanel>

            <!--Gridsplitter 1-->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="DarkGray"/>

            <!--middle menu-->
            <ListView ItemsSource="{Binding Items1}" Grid.Column="2">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Code}" Header="Name"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Created"/>
                        <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Password" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!--Gridsplitter 2-->
            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="DarkGray"/>

            <!--Log here-->
            <materialDesign:Card Grid.Column="4" VerticalAlignment="Stretch">
                <StackPanel>
                    <TextBlock Style="{StaticResource InformationTextblockStyle}" Text="Task-log:"/>
                </StackPanel>
            </materialDesign:Card>

        </Grid>
    </Grid>
</UserControl>
